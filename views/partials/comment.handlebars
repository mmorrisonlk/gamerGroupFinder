<div class="row">
  <div class="col s12 m6">
    <div class="card">
      <div class="card-content">
        <p>{{content}}</p>
        <p>--{{User.name}} on {{format_date createdAt}}</p>
      </div>
    </div>
  </div>
</div>