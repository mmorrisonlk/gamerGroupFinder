<div class="row search-field">
    <div class="col s12">
        <div class="row">
            <div class="input-field col l4">
                <input type="text" id="searchField">
                <label for="searchField">Search</label>
            </div>
            <button class="btn waves-effect waves-light search-btn" type="submit" name="action">
                <i class="material-icons">send</i>
            </button>
        </div>
    </div>
</div>

<div class="container indigo darken-3 white-text">
    <div class="row">
        <div class="col s6">Content</div>
        <div class="col s2 right">Difficulty</div>
        <div class="col s2 right">Current / Max Players</div>
        <div class="col s2 right">Game Name</div>
    </div>
</div>


{{#each groups as |group| }}

<div class="container indigo lighten-4">
    <a href='/group/{{group.id}}' class="group-link">
        <div class="row">
            <div class="col s6">{{content}}</div>
            <div class="col s2 right">{{dificulty}}</div>
            <div class="col s2 right">{{currentPlayers}} / {{maxPlayers}} People</div>
            <div class="col s2 right">{{gameName}}</div>
        </div>
    </a>
</div>

    
{{/each}}