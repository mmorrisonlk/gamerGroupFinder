<div class="row">
    <div class="col s12">
        <div class="row">
            <div class="input-field col l4 search-field">
                <input type="text" id="searchField">
                <label for="searchField">Search</label>
            </div>
        </div>
    </div>
</div>

{{!-- <!-- Loop over each object in the projects array -->
{{#each projects as |project| }} --}}
<div class="container">
    <div class="row">
        <div class="col s1">Picture</div>
        <div class="col s5">Description</div>
        <div class="col s2 right">Casual</div>
        <div class="col s2 right">3 / 5 People</div>
        <div class="col s2 right">Shooter</div>
    </div>
</div>
    
{{!-- {{/each}} --}}